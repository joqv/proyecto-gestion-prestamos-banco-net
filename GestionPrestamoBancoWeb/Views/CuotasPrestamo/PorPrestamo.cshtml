@model IEnumerable<GestionPrestamoBancoWeb.Models.CuotaPrestamo>

@{
    ViewData["Title"] = "PorPrestamo";
    var prestamo = ViewBag.Prestamo as GestionPrestamoBancoWeb.Models.Prestamo;
    var cuentas = ViewBag.Cuentas as List<GestionPrestamoBancoWeb.Models.CuentaBancaria>;
}

<h1>Cuotas por Préstamo</h1>

@* <p>
    <a asp-action="Create">Create New</a>
</p> *@
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.IdCuota)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdPrestamo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NumeroCuota)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FechaVencimiento)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MontoTotalCuota)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MontoCapital)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MontoInteres)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SaldoPendiente)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MontoPagado)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EstadoCuota)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IdCuota)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdPrestamo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NumeroCuota)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FechaVencimiento)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MontoTotalCuota)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MontoCapital)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MontoInteres)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SaldoPendiente)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MontoPagado)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EstadoCuota)
            </td>
            <td>
                @if (item.EstadoCuota == "PENDIENTE")
                {
                        @Html.ActionLink("Pagar", "PagarCuota", "CuotasPrestamo", new { idPrestamo = item.IdPrestamo, idCuota = item.IdCuota }, new { @class = "btn btn-primary btn-sm" })
                    @* <form asp-controller="CuotasPrestamo" asp-action="PagarCuota" method="post">
                        <input type="hidden" name="IdCuota" value="@item.IdCuota" />
                        <input type="hidden" name="IdPrestamo" value="@item.IdPrestamo" />
                        <input type="hidden" name="MontoAPagar" value="@item.SaldoPendiente" />
                        <input type="hidden" name="IdCuentaOrigen" value="123456" />
                        <select name="IdCuentaOrigen" class="form-control mb-2">
                                @if (cuentas != null)
                                {
                                    @foreach (var cuenta in cuentas)
                                    {
                                        <option value="@cuenta.IdCuenta">Cuenta #@cuenta.NumeroCuenta (@cuenta.Saldo.ToString("C") )</option>
                                    }
                                }

                        </select>
                        <button type="submit" class="btn btn-primary">Pagar</button>
                    </form> *@
                }
                @* @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ }) *@
            </td>
        </tr>
}
    </tbody>
</table>
